import { FontScrollDirective } from './font-scroll.directive';

describe('FontScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new FontScrollDirective();
    expect(directive).toBeTruthy();
  });
});
