import { SupplyLimitPipe } from './supply-limit.pipe';

describe('SupplyLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new SupplyLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
